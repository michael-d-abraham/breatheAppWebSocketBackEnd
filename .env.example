# Vue app (Vite) — WebSocket URL. Rebuild after changing for production.
#
# Local dev:
# VITE_WS_URL=ws://localhost:8085
#
# Production (HTTPS site must use secure WebSocket):
# VITE_WS_URL=wss://your-service-name.onrender.com

VITE_WS_URL=ws://localhost:8085
